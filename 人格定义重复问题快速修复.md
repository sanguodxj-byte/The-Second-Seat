# 人格定义重复问题快速修复指南

## ?? 问题症状

```
Mod yourname.thesecondseat has multiple NarratorPersonaDefs named Sideria_Default. Skipping.
```

---

## ?? 一键修复

### 方法 1: PowerShell 脚本

```powershell
# 删除异常目录
Remove-Item "D:\steam\steamapps\common\RimWorld\Mods\TheSecondSeat\Defs\ers" -Recurse -Force -ErrorAction SilentlyContinue

# 验证修复
Get-ChildItem "D:\steam\steamapps\common\RimWorld\Mods\TheSecondSeat\Defs" -Filter "*.xml" -Recurse | Select-Object FullName
```

### 方法 2: 手动删除

1. 打开文件夹：
   ```
   D:\steam\steamapps\common\RimWorld\Mods\TheSecondSeat\Defs
   ```

2. 删除任何异常目录（如 `ers`、嵌套路径）

3. 确保只保留：
   - ? `GameComponentDefs.xml`
   - ? `NarratorPersonaDefs.xml`
   - ? `NarratorPersonaDefs\` 文件夹

---

## ? 验证修复

### 检查命令

```powershell
# 搜索 Sideria_Default
Get-ChildItem "D:\steam\steamapps\common\RimWorld\Mods\TheSecondSeat\Defs" `
    -Recurse -Filter "*.xml" | Select-String "Sideria_Default" -List

# 预期：只有 1 个结果
```

### 预期结果

```
? 只有 1 个文件包含 Sideria_Default
? 路径正常（无嵌套）
? 重启游戏无错误
```

---

## ?? 重启游戏

```
1. 完全关闭 RimWorld
2. 重新启动
3. 加载存档
4. 测试功能
```

---

**修复完成！** ?

_The Second Seat Mod Team_
